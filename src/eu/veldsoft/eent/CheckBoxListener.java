/*
 * CheckBoxListener.java
 *
 * Created on April 16, 2003, 8:40 PM
 */
/*
 It is distributed under the GNU Public Licence (GPL) version 2.  See
 http://www.gnu.org/ for further details of the GPL.
 */
package eu.veldsoft.eent;

/**
 *
 * @author  MK
 */

import java.awt.event.ItemEvent;
import java.awt.event.ItemListener;

public class CheckBoxListener implements ItemListener {

	/*
	 * Check if the Checkbox is selected if selected remove the tile from player
	 * and board
	 */
	public void itemStateChanged(ItemEvent e) {
	}
	/*
	 * if ( e.getSource() == Tigris2.jCheckBox1 ) { if (e.getStateChange() ==
	 * ItemEvent.SELECTED ) {
	 * 
	 * // Board.currentPlayer.removeCivTile(0); //
	 * Tigris2.uic[0].removePlayerTile();
	 * 
	 * } } if ( e.getSource() == Tigris2.jCheckBox2 ) { if (e.getStateChange()
	 * == ItemEvent.SELECTED ) { // // Board.currentPlayer.removeCivTile(1); //
	 * Tigris2.uic[1].removePlayerTile(); } }
	 * 
	 * if ( e.getSource() == Tigris2.jCheckBox3 ) { if (e.getStateChange() ==
	 * ItemEvent.SELECTED ) {
	 * 
	 * // Board.currentPlayer.removeCivTile(2); //
	 * Tigris2.uic[2].removePlayerTile(); } }
	 * 
	 * if ( e.getSource() == Tigris2.jCheckBox4 ) { if (e.getStateChange() ==
	 * ItemEvent.SELECTED ) {
	 * 
	 * // Board.currentPlayer.removeCivTile(3); //
	 * Tigris2.uic[3].removePlayerTile(); } }
	 * 
	 * if ( e.getSource() == Tigris2.jCheckBox5 ) { if (e.getStateChange() ==
	 * ItemEvent.SELECTED ) {
	 * 
	 * // Board.currentPlayer.removeCivTile(4); //
	 * Tigris2.uic[4].removePlayerTile(); } }
	 * 
	 * if ( e.getSource() == Tigris2.jCheckBox6 ) { if (e.getStateChange() ==
	 * ItemEvent.SELECTED ) {
	 * 
	 * // Board.currentPlayer.removeCivTile(5); //
	 * Tigris2.uic[5].removePlayerTile(); } }
	 * 
	 * }
	 */
}
